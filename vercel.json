{
    "builds": [
        {
            "src": "/server/api.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "server/api.py"
        }
    ]
}
